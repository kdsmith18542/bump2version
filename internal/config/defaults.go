// Package config provides configuration management for bumpx.
package config

// Default values for configuration
const (
	DefaultConfigFile = ".bumpx.toml"
)

// Default project settings
var (
	DefaultVersionScheme = "semver"
	DefaultCurrentVersion = "0.1.0"
)